# 更新驗證紀錄

驗證日期：2026-08-21

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 匯率載入 | 通過 | Currency API 成功載入 9 張預設貨幣卡片，狀態列顯示來源與最後更新時間。 |
| 幣別精度 | 通過 | USD 顯示兩位小數；JPY 顯示為整數。 |
| 計算器 | 通過 | `1000*0.12752` 得到 `127.52`；`1+2*3` 得到 `7`；`10/0` 會被拒絕並回傳「不可除以零」。 |
| PWA 設定 | 通過 | Manifest 已連結，Service Worker 已在本機 HTTP 伺服器上啟用。 |
| 離線備援 | 通過 | 模擬網路失敗後，應用程式讀取本機快取匯率並顯示明確的快取資料狀態。 |

本驗證於 `http://localhost:8080/` 執行。實際部署後，仍應在正式 HTTPS 網域上再次確認安裝提示與 Service Worker 更新流程。

## currency4 版面整合驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 新版介面呈現 | 通過 | 淺色終端機外觀、彩色緊湊幣別卡片、國旗與貨幣縮寫均正常呈現。 |
| 預設貨幣 | 通過 | 成功載入 20 種預設貨幣卡片。 |
| 資料來源 | 通過 | 使用者版本中的四組內嵌服務金鑰未被帶入；頁面僅保留兩個不需金鑰的公開來源。 |
| 計算器 | 通過 | `1+2*3` 得到 `7`，`1000*0.12752` 得到 `127.52`，`10/0` 被拒絕。 |
| PWA | 通過 | Service Worker 在本機 HTTP 測試環境中為啟用狀態。 |

| 資料來源切換 | 通過 | 切換至 Open ER API 後，20 種預設貨幣重新載入，且選擇已保存於瀏覽器。 |
| 新版離線回退 | 通過 | 模擬 Open ER API 無法連線時，應用程式使用最近快取資料並標示快取狀態。 |

## 效能最佳化驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 靜態最佳化檢查 | 通過 | 已檢查快取有效期限、快取預先渲染、批次 DOM 更新、動畫幀拖曳節流、縮小國旗資源與 PWA 快取版本。 |
| 線上資料同步 | 通過 | 在本機頁面選用 Open ER API 後，背景同步完成並成功呈現 20 種預設貨幣。 |
| 縮小國旗資源 | 通過 | 最新 App Shell 以 `w40` 國旗資源提供所有可見貨幣卡片。 |
| 快取預先渲染 | 通過 | 清空清單後以已快取匯率立即還原 20 張貨幣卡片，並標示背景同步狀態。 |
| 局部金額更新 | 通過 | 更新基準金額時，卡片節點維持不變，僅更新金額欄位，無需重建整個清單。 |

## 頂部圖示工具列驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 圖示工具列呈現 | 通過 | 重新整理、編輯、新增、安裝與匯率來源控制項均已移至頂部並以無文字圖示按鈕呈現。 |
| PWA 更新交接 | 觀察 | 已安裝快取中的舊版 `app.js` 與新版頁面首次交接時，需重新載入一次以套用新版主程式；將在完成更新後驗證重新載入行為。 |
| 匯率來源圖示選單 | 通過 | 頂部來源圖示可開啟含 Currency API 與 Open ER API 的選單，且按鈕會以目前選定來源提供可讀取標籤。 |
| 新版 PWA 資產交接 | 通過 | 重新載入後，頁面與主程式可同時載入新版工具列與 20 張貨幣卡片。 |
| 匯率來源切換 | 通過 | 透過圖示選單切換至 Currency API 後，選單收合、偏好已保存，頂部圖示的無障礙標籤更新為「匯率來源：Currency API」。 |

## 貨幣名稱顯示驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 完整貨幣名稱 | 通過 | 20 張預設貨幣卡片均以雙行方式顯示代碼與完整英文貨幣名稱，例如 HKD／Hong Kong Dollar。 |
| 卡片可讀性 | 通過 | 卡片高度與國旗尺寸同步調整，完整名稱不再作為低對比輔助水印呈現。 |

## 單行貨幣名稱驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 單行卡片文字 | 通過 | 貨幣代碼與完整名稱在同一行呈現，例如 HKD Hong Kong Dollar；金額仍靠右對齊。 |

## 熱門貨幣釘選驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 初始清單載入 | 通過 | 更新後頁面可正常載入 20 張貨幣卡片與既有單行名稱版面。 |
| 釘選與熱門排序 | 通過 | 在編輯模式釘選 USD 後，USD 立即移至清單最上方，卡片顯示星號，控制項改為「取消釘選」。 |
| 釘選偏好保存 | 通過 | 重新載入後，本機儲存的 `USD` 釘選偏好仍存在；USD 保持在第一張卡片並顯示星號，20 張卡片完整呈現。 |

## 卡片色彩與底部資訊驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 卡片配色 | 通過 | 卡片飽和度與亮度已各降低約 5 個百分點，顏色保持辨識度但較柔和。 |
| 底部資訊列 | 通過 | 最後更新時間與匯率參考說明已移至所有貨幣卡片之後的頁面最底部。 |

## 雙行貨幣名稱與色彩驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 完整貨幣名稱 | 通過 | 貨幣代碼與完整名稱已上下分為兩行；United States Dollar、South Korean Won 等長名稱均完整可見。 |
| 卡片色彩 | 通過 | 卡片飽和度已較原始設定降低 15 個百分點、亮度降低至 55%，在保留分類色彩下進一步降低視覺強度。 |

## 緊湊單行與淺色卡片驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 緊湊單行版面 | 通過 | 卡片高度縮減為 46px，貨幣代碼、釘選星號與名稱已同行排列。 |
| 淺色配色 | 通過 | 卡片使用較低飽和度與 74% 亮度的淡色配色，仍可區分不同貨幣。 |

## 40px 卡片與 60% 亮度配色驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 卡片高度 | 通過 | 桌面與行動版卡片高度均設定為 40px，20 種貨幣能在單一視圖中更緊湊呈現。 |
| 卡片配色 | 通過 | 卡片採用較原始顏色低 35 個百分點的飽和度與 60% 亮度，符合低飽和度淡色設定。 |

## 固定 RGB 不重複配色驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 指定 RGB 色表 | 通過 | 貨幣卡片採用使用者指定維基百科 RGB 顏色列表中的固定淺色系十六進位色碼。 |
| 不重複配色 | 通過 | 瀏覽器實測 20 張卡片具有 20 種不同的 RGB 背景色，沒有重複。 |

## 多來源匯率快取驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 來源分流快取建立 | 通過 | Currency API 的快取已建立於 `fx_terminal_rate_caches_v2`，並保有獨立來源鍵。 |
| 卡片資料完整性 | 通過 | 快取包含 340 個可用匯率；20 張已顯示貨幣卡片均可由同一份快取資料呈現。 |
| 快取時間分離 | 通過 | 本機快取寫入時間與來源資料更新時間分開保存，背景更新判斷不會受每日來源時間戳影響。 |
| 新鮮快取重新載入 | 通過 | 重新載入後立即呈現 20 張卡片，頁面狀態顯示快速快取期限內，且本次載入沒有匯率 API 資源請求。 |

## 深色／淺色主題驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 頂部主題控制 | 通過 | 頂部工具列顯示主題圖示；切換後圖示與無障礙提示會改為相反模式。 |
| 深色可讀性 | 通過 | 終端背景、工具列、20 張貨幣卡片、金額文字與底部資訊皆切換至深色對比樣式。 |
| 卡片色彩 | 通過 | 深色模式將固定 RGB 淺色卡片調整為較深色調，仍維持每種貨幣的色彩辨識。 |
| 主題偏好保存 | 通過 | 重新載入後 `fx_terminal_theme`、文件主題與切換按鈕均為深色狀態；20 張卡片仍完整呈現，金額文字為高對比淺色。 |

## 深色低飽和度與編輯控制修正驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 深色低飽和度卡片 | 通過 | 深色模式的各貨幣卡片改為柔和的低飽和度深灰色調，仍保留不同貨幣之間的細微辨識。 |
| 編輯完成控制 | 通過 | 進入編輯模式後，頂部編輯按鈕改為 34px 內的勾選圖示；無障礙提示為「完成編輯貨幣清單」，不再顯示或溢出「完成」文字。 |

## 深色模式高飽和度淡色配色驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 貨幣卡片配色 | 通過 | 深色主題下的 20 張貨幣卡片均轉為目標亮度 80%、飽和度 72% 的淡色背景；瀏覽器量測亮度為 80%、飽和度約 73%，色相保持彼此可辨識。 |
| 工具列按鈕 | 通過 | 深色主題的圖示按鈕採淡藍色背景，背景色為 `rgb(201, 229, 247)`；編輯模式的完成按鈕採淡粉色背景 `rgb(243, 183, 194)`，圖示維持深色高對比。 |
| 編輯控制 | 通過 | 完成控制仍為 34 × 34px 勾選 SVG 圖示，無文字顯示或按鈕溢出；淡粉色懸停規則已確認不會被一般按鈕的淡藍色狀態覆蓋。 |

## 卡片密度與色彩主題驗證

| 測試項目 | 結果 | 證據／觀察 |
|---|---|---|
| 偏好面板入口 | 通過 | 頂部工具列新增無文字的設定圖示，開啟後顯示「卡片密度」與「卡片配色」兩個區段。 |
| 可選項目 | 通過 | 面板提供「緊湊／舒適」兩種密度，以及「鮮明／柔和／極簡」三組卡片配色。 |
| 舒適密度 | 通過 | 切換後卡片高度由 36px 增至 48px，旗幟與文字同步放大；工具列與偏好面板維持可用。 |
| 柔和配色 | 通過 | 切換後所有貨幣卡片即時改為較低飽和、仍保有色相差異的淡色配色，金額與文字維持深色對比。 |
| 偏好持久化 | 通過 | 瀏覽器量測 `fx_terminal_density=comfortable`、`fx_terminal_card_theme=soft`；重新載入後資料屬性與卡片高度仍分別為 `comfortable`、`soft` 及 48px。 |
| 極簡配色 | 通過 | 極簡主題可將卡片轉為近中性的灰階淡色背景，同時仍保留各貨幣間的微小色相差異與文字可讀性。 |
